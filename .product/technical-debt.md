# Technical Debt — graduation_project (Signlingo)

> Auto-generated by /analyze-codebase on 2026-05-08.

## Summary

- **TODO/FIXME comments:** 5 (across 5 files — very low)
- **Source files (`lib/**/*.dart`):** 137
- **Test files:** 1 (default `widget_test.dart` scaffold only) → **<1% test coverage**
- **Files over 10KB:** 11 (no hard line-count threshold — Dart files use bytes here)
- **CI/CD configured:** no
- **Custom lints configured:** no (using `flutter_lints` defaults)
- **Dependency vulnerability scan:** not run (Flutter has no `npm audit` equivalent; consider `dart pub outdated --mode=null-safety` and `flutter pub deps`)

## High-priority debt (worth fixing soon)

### 1. Hardcoded backend URL scattered across 6+ files

`https://signlingo.org/api/` is duplicated as a literal in:

- [lib/business_logic/Auth/OtpCubit/otp_cubit.dart:12,18,40,72](lib/business_logic/Auth/OtpCubit/otp_cubit.dart#L12)
- [lib/business_logic/Auth/LoginCubit/login_cubit.dart:14,25](lib/business_logic/Auth/LoginCubit/login_cubit.dart#L14)
- [lib/business_logic/Auth/SignUpCubit/SignUpCubit.dart:8,20](lib/business_logic/Auth/SignUpCubit/SignUpCubit.dart#L8)
- [lib/business_logic/Auth/ForgetPasswordCubit/forget_password_cubit.dart:9,18](lib/business_logic/Auth/ForgetPasswordCubit/forget_password_cubit.dart#L9)
- [lib/data/Services/UserApiService.dart:15,35](lib/data/Services/UserApiService.dart#L15)
- [lib/data/Services/QuizApiService.dart:11](lib/data/Services/QuizApiService.dart#L11)

**Fix:** centralize in a single `ApiConfig` class (e.g., `lib/Core/Config/api_config.dart`) and make it switchable per build flavor (dev / prod).

### 2. Repository layer almost completely missing

`lib/data/Repos/` contains only `AuthRepo.dart`. Every other feature's cubit calls `Dio` directly. This conflicts with the layered structure implied by the folder names and makes the cubits hard to unit-test (no seam to mock).

**Fix candidates:** add `LessonsRepo`, `QuizRepo`, `DictionaryRepo`, `ProfileRepo`, `CategoriesRepo`, and have the corresponding cubits depend on them.

### 3. No real test suite

Only `test/widget_test.dart` (the default Flutter scaffold) exists. With 14 cubits, the highest leverage tests would be `bloc_test`-driven cubit tests. Consider adding `bloc_test` and `mocktail` to `dev_dependencies`.

### 4. Build artifacts and crash logs at repo root

Currently untracked but cluttering the working tree:

- `build_output.txt`, `build_output2.txt`, …, `build_output5.txt`
- `hs_err_pid11992.log`, `hs_err_pid17360.log`, `hs_err_pid39276.log` (JVM crash dumps — `*.log` already in `.gitignore`, so safe)
- `replay_pid*.log` (also `*.log`-matched)

**Fix:** add `build_output*.txt` to `.gitignore`, then delete all of these from the working tree.

### 5. ML model file in wrong location

`hand_landmarker.task` (~hundreds of KB / few MB) is sitting at the **repo root**, not under `Assets/models/`. It is checked in (untracked → `??` in git status, so on next commit it will be staged from root).

**Fix:** move to `Assets/models/hand_landmarker.task` and update `pubspec.yaml` assets section + any path references in code.

## Medium-priority

### 6. `lib/testmediapipe.dart` — orphan scratch file

Looks like dev scratch code. Either delete or move into `test/` / `example/`.

### 7. TODO in main.dart

```dart
listener: (context, state) {
  // TODO: implement listener
}
```
[lib/main.dart:39](lib/main.dart#L39) — empty BlocConsumer listener at the app root. Either delete the listener (use `BlocBuilder` instead) or implement it.

### 8. Generated files committed

`lib/generated/l10n.dart` (~50KB) and `lib/generated/intl/messages_{ar,en}.dart` together account for ~107KB of the codebase. Common to gitignore these for `flutter_intl` projects since they regenerate from `.arb` files.

## Largest files

| File | Bytes | Notes |
|------|-------|-------|
| lib/generated/l10n.dart | 51,703 | generated — see #8 |
| lib/generated/intl/messages_ar.dart | 30,484 | generated |
| lib/generated/intl/messages_en.dart | 25,707 | generated |
| lib/presentation/Quiz/Quiz.dart | 16,535 | could split widgets |
| lib/presentation/Profile/ProfileScreen2.dart | 15,577 | could split widgets |
| lib/presentation/PlayVideo/VideoScreen.dart | 14,216 | |
| lib/presentation/VideoTranslation/handtrackingview.dart | 14,167 | currently modified |
| lib/presentation/Profile_information/Profile_information.dart | 12,452 | |
| lib/presentation/VoiceTranslation/voice_translation_page.dart | 12,099 | new file |
| lib/presentation/QuickRespose/quickresponse.dart | 11,284 | |

## Untested feature inventory

All 9 business-logic features (14 cubits) have **zero** test coverage. Highest-value candidates for first tests:
- `SendFramesCubit` — drives the camera/ONNX inference loop, expensive to debug manually
- `LoginCubit` / `SignUpCubit` / `OtpCubit` — auth correctness matters and is hard to verify by clicking
- `QuizCubit` — has scoring logic that's easy to unit-test
