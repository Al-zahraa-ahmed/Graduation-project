# Technical Debt — graduation_project (Signlingo)

> Auto-generated by /analyze-codebase. Last full scan 2026-05-08; incrementally refreshed on **2026-06-18** (HEAD `fa06aa3` — audit batch committed; Google Sign-In + connectivity gate added).

## Summary

- **TODO/FIXME comments:** **0** (down from 3)
- **Source files (`lib/**/*.dart`):** **144** (+7; GoogleAuth, QuickResponse, ConnectivityGate, others)
- **Test files:** 1 (default `widget_test.dart` scaffold only) → **<1% test coverage** — *unchanged, now the single biggest gap*
- **`print(...)` debug calls:** **46** (new debt — GoogleAuthCubit alone ships ~20; should be stripped or behind `kDebugMode` before release)
- **Hardcoded API URL literals:** **19** across the codebase (grew from the 6+ files noted before — debt #1 got worse)
- **Files over 300 LOC:** 11
- **CI/CD configured:** no
- **Custom lints configured:** no (using `flutter_lints` defaults)
- **Uncommitted work:** Google Sign-In feature folder is untracked; 11 other files modified — smaller risk than before, but commit it before it grows.

## Recently resolved

- **#5 ML model file in wrong location** — `hand_landmarker.task` moved from repo root → `Assets/models/` in commit `ef92bb5`. Verified missing at root.
- **#7 TODO in main.dart** — empty `BlocConsumer` listener replaced with a `BlocBuilder` in the 2026-05-23 working tree. (Working-tree-only; the fix is not in any commit yet — see #9.)
- **Login/SignUp cubit + form hardening** — both flows now have isClosed checks, response-shape validation, ApiException-friendly messages, Dio 15s/30s/30s timeouts, concurrent-call guard, `close()` cancels in-flight requests, autovalidate-after-first-failure, focus-node flow, in-button spinner, retry-on-error snackbar, WidgetState* deprecation migration. (Working tree only.)
- **UserModel round-trip bugs** — `toJson()` was dropping `language`/`mode`/`theme`/`img`; `copyWith()` had a dead `phone` param and was missing `img`/`isVerified`. Both fixed. (Working tree only.)
- **CustomTextField visual bug** — input border now matches container radius (both 16); validator error renders outside the white container via `FormField<String>` wrapper. (Working tree only.)
- **`ApiExceptions` upgraded** — per-`DioExceptionType` mapping, status-code mapping (400/401/403/404/409/422/429/5xx), Laravel `errors`-map flattening. (Working tree only.)

## High-priority debt (worth fixing soon)

### 1. Hardcoded backend URL scattered across 6+ files

`https://signlingo.org/api/` is duplicated as a literal in:

- [lib/business_logic/Auth/OtpCubit/otp_cubit.dart:12,18,40,72](lib/business_logic/Auth/OtpCubit/otp_cubit.dart#L12)
- [lib/business_logic/Auth/LoginCubit/login_cubit.dart:14,25](lib/business_logic/Auth/LoginCubit/login_cubit.dart#L14)
- [lib/business_logic/Auth/SignUpCubit/SignUpCubit.dart:8,20](lib/business_logic/Auth/SignUpCubit/SignUpCubit.dart#L8)
- [lib/business_logic/Auth/ForgetPasswordCubit/forget_password_cubit.dart:9,18](lib/business_logic/Auth/ForgetPasswordCubit/forget_password_cubit.dart#L9)
- [lib/data/Services/UserApiService.dart:15,35](lib/data/Services/UserApiService.dart#L15)
- [lib/data/Services/QuizApiService.dart:11](lib/data/Services/QuizApiService.dart#L11)

**Fix:** centralize in a single `ApiConfig` class (e.g., `lib/Core/Config/api_config.dart`) and make it switchable per build flavor (dev / prod).

### 2. Repository layer almost completely missing

`lib/data/Repos/` contains only `AuthRepo.dart`. Every other feature's cubit calls `Dio` directly. This conflicts with the layered structure implied by the folder names and makes the cubits hard to unit-test (no seam to mock).

**Fix candidates:** add `LessonsRepo`, `QuizRepo`, `DictionaryRepo`, `ProfileRepo`, `CategoriesRepo`, and have the corresponding cubits depend on them.

### 3. No real test suite

Only `test/widget_test.dart` (the default Flutter scaffold) exists. With 14 cubits, the highest leverage tests would be `bloc_test`-driven cubit tests. Consider adding `bloc_test` and `mocktail` to `dev_dependencies`.

### 4. Build artifacts and crash logs at repo root

Currently untracked but cluttering the working tree:

- `build_output.txt`, `build_output2.txt`, …, `build_output5.txt`
- `hs_err_pid11992.log`, `hs_err_pid17360.log`, `hs_err_pid39276.log` (JVM crash dumps — `*.log` already in `.gitignore`, so safe)
- `replay_pid*.log` (also `*.log`-matched)

**Fix:** add `build_output*.txt` to `.gitignore`, then delete all of these from the working tree.

### 5. ~~ML model file in wrong location~~ ✅ RESOLVED in `ef92bb5`

`hand_landmarker.task` was moved to `Assets/models/`. Pubspec assets and code references were updated.

## Medium-priority

### 6. `lib/testmediapipe.dart` — orphan scratch file

Looks like dev scratch code. Either delete or move into `test/` / `example/`.

### 7. ~~TODO in main.dart~~ ✅ RESOLVED (working tree, uncommitted)

`BlocConsumer` with empty listener was replaced by a `BlocBuilder`. Fix is in the working tree but not yet committed — gets locked in when the audit-pass batch lands.

### 8. Generated files committed

`lib/generated/l10n.dart` (~50KB, ~2,113 lines) and `lib/generated/intl/messages_{ar,en}.dart` together account for ~107KB of the codebase. Common to gitignore these for `flutter_intl` projects since they regenerate from `.arb` files.

### 9. ~~Massive uncommitted batch~~ ✅ LARGELY RESOLVED in `fa06aa3`

The 43-file audit-pass batch landed as commit `fa06aa3`; `AppSnackBar.dart` and `Validators.dart` are now tracked. Residual risk: the new **Google Sign-In** folder is again untracked and 11 files are modified — don't let a second large uncommitted batch accumulate. **Still verify** whether `fa06aa3` has been *pushed* to `origin/main` (the Windows Credential Manager wrong-account blocker in `progress.md` may still be unresolved — `git log origin/main..HEAD` to check).

### 11. **NEW** — Debug `print()` logging in production paths

`GoogleAuthCubit` is instrumented with ~20 raw `print('[GoogleAuth] step N…')` calls (each `// ignore: avoid_print`-suppressed), including printing the backend response body and idToken length. 46 `print()` calls exist across `lib/` total. These leak to release logcat/console and add noise.

**Fix:** gate behind `if (kDebugMode)`, or route through a `Logger` abstraction; never log token material.

### 12. **NEW** — Per-cubit ad-hoc Dio instances, duplicated config

`LoginCubit`, `SignUpCubit`, `GoogleAuthCubit`, `ForgetPasswordCubit`, `CategoriesCubit`, `SearchCubit` each construct their own `Dio` with the base URL (and sometimes timeouts) inline — 19 URL literals total. `GoogleAuthCubit` and `LoginCubit` duplicate the exact same `BaseOptions(connectTimeout 15s / receive 30s / send 30s)`. Compounds debt #1 (centralize URL) and #2 (no repo layer). One shared configured `Dio`/`ApiService` injected into cubits would collapse all of this.

### 10. **NEW** — `OtpCubit` / `ForgetPasswordCubit` / `ResetPasswordCubit` haven't received the full hardening

LoginCubit + SignUpCubit got the complete hardening pattern (isClosed checks, ApiException mapping, timeouts, response-shape validation, close()→dio.close). The other three Auth cubits are modified in the working tree but per `progress.md` next-steps #4 the broader hardening is **explicitly deferred** there. Inconsistent — at minimum the timeout and isClosed-check parts should be uniform across Auth.

## Largest files (by line count)

| Lines | File | Notes |
|------:|------|-------|
| 2,113 | lib/generated/l10n.dart | generated — see #8 |
|   470 | lib/presentation/Quiz/Quiz.dart | could split widgets |
|   445 | lib/presentation/Profile/ProfileScreen2.dart | could split widgets |
|   436 | lib/presentation/PlayVideo/VideoScreen.dart | grew +20 lines since 2026-05-08 |
|   435 | lib/generated/intl/messages_en.dart | generated |
|   433 | lib/generated/intl/messages_ar.dart | generated |
|   365 | lib/presentation/VoiceTranslation/voice_translation_page.dart | shipped in `ef92bb5` |
|   357 | lib/presentation/QuickRespose/quickresponse.dart | |
|   354 | lib/presentation/VideoTranslation/VideoTranslationScreen.dart | |
|   346 | lib/presentation/Profile_information/Profile_information.dart | |
|   306 | lib/presentation/VideoTranslation/handtrackingview.dart | rewrote in `ef92bb5` |
|   230 | lib/data/Services/sign_language_classifier.dart | new in `ef92bb5` (ONNX inference) |

## Untested feature inventory

All 9 business-logic features (14 cubits) have **zero** test coverage. Highest-value candidates for first tests:
- `SendFramesCubit` — drives the camera/ONNX inference loop, expensive to debug manually
- `LoginCubit` / `SignUpCubit` / `OtpCubit` — auth correctness matters and is hard to verify by clicking
- `QuizCubit` — has scoring logic that's easy to unit-test
- `VoiceTranslationCubit` — speech_to_text state machine, easy to fake the recognizer

## In-flight refactor pass (uncommitted at HEAD `ef92bb5`, status as of 2026-05-23)

Two new shared utilities introduced 2026-05-08 are now consumed by the auth forms, but both are **still untracked** in git and at risk:

| File | Purpose | Already used by | Status |
|------|---------|-----------------|--------|
| `lib/Core/CustomWidgets/AppSnackBar.dart` | Centralized `error / success / info` snackbar with retry action and `clearSnackBars()` (avoids the stuck-snackbar animation race) | LoginForm, RegistrationForm (and likely OtpForm + forget-password forms — verify before committing) | untracked for 15 days |
| `lib/Core/Validators.dart` | Shared form validators (`email`, `password` min-8, `confirmPassword`, `name`) returning localized error strings via passed-in message params | LoginForm, RegistrationForm | untracked for 15 days |

**Screen-by-screen audit walkthrough progress** (per `progress.md`): visited Onboarding ✅, ChooseAppMode ✅, LoginContainer ✅, SignUp form ✅. Remaining order: WelcomePage → OTP → ForgetPassword (3 screens) → LearningHome → TranslationHome → Categories → Lessons → Quiz + QuizResult → QuickPractice + QuickResponse → Dictionary → PlayVideo → VideoTranslation (5 screens) → VoiceTranslation → Profile + ProfileInformation → About / Help / Legal (5 static) → Error screens (NoConnection, NotFound).

Migration is partial — verify all auth/forms screens consume `Validators` and `AppSnackBar` before considering the auth audit complete. Then commit `AppSnackBar.dart` and `Validators.dart` together with the screens that depend on them so the repo is internally consistent.
