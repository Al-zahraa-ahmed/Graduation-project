# Deployment Status — graduation_project (Signlingo)

> Auto-generated by /analyze-codebase. Last full scan 2026-05-08; incrementally refreshed on **2026-05-23** (HEAD still `ef92bb5`, working tree dirty — auth-hardening batch uncommitted).

This is a Flutter **client app**, so "deployment" means publishing to app stores (Android / iOS) and/or distributing platform-specific builds (Web/Windows/macOS/Linux). The backend `signlingo.org` is hosted **outside this repo**.

## Detected Infrastructure

| Component | Platform | Config | Status |
|-----------|----------|--------|--------|
| Android build | Gradle (Kotlin DSL — `build.gradle.kts`) | [android/app/build.gradle.kts](android/app/build.gradle.kts), [android/gradle/wrapper/gradle-wrapper.properties](android/gradle/wrapper/gradle-wrapper.properties) | local builds work, no signing config detected; both files modified in working tree on 2026-05-23 (likely Gradle wrapper bump — verify version before committing) |
| iOS build | Xcode | [ios/Runner/Info.plist](ios/Runner/Info.plist), `ios/Runner.xcodeproj` | local builds work, no fastlane / App Store config |
| Web build | Flutter web | [web/index.html](web/index.html) | scaffold only, untested |
| Windows / macOS / Linux | Flutter desktop | platform folders present | scaffolds only, untested |
| CI/CD | — | none | no `.github/workflows`, no `.gitlab-ci.yml`, no Codemagic / Bitrise config |
| Backend | external — `https://signlingo.org/api/` | not in this repo | unknown to this analysis |
| Code signing | — | none committed | needs setup before store release |
| App-store metadata | — | none | no Fastlane, no `metadata/` |
| Crash reporting | — | none | consider Firebase Crashlytics or Sentry |
| Analytics | — | none | none detected |

## Environment / Configuration

No `.env` or `.env.example` files. Configuration today is:

| Config | Where | Notes |
|--------|-------|-------|
| API base URL | hardcoded in 6+ Dart files | should be centralized — see `technical-debt.md` #1 |
| Token | `CacheHelper` (shared_preferences) key `'token'` | survives app restarts |
| Language | `CacheHelper` key `'lang'` (`'ar'` / `'en'`) | falls back to `'en'` |
| App mode | `CacheHelper` key `'mode'` (`'l'` for learning, else translation) | set on `ChooseAppMode` screen |
| ML model paths | `Assets/models/sign_lstm_attention_fp32.onnx` and `Assets/models/hand_landmarker.task` (both in pubspec assets) | |

## Required device permissions

Inferred from features (verify exact strings in [AndroidManifest.xml](android/app/src/main/AndroidManifest.xml) and [Info.plist](ios/Runner/Info.plist), both currently modified and uncommitted):

- **CAMERA** — VideoTranslation feature
- **RECORD_AUDIO / NSMicrophoneUsageDescription** — VoiceTranslation (speech_to_text)
- **READ_MEDIA_IMAGES / NSPhotoLibraryUsageDescription** — image_picker (Profile)
- **INTERNET** — Dio API calls

## Build Commands

```bash
# Install deps
flutter pub get

# Regenerate localizations after editing .arb files
flutter pub run intl_utils:generate

# Dev (debug) — connected device or emulator
flutter run

# Android release APK / AAB
flutter build apk --release
flutter build appbundle --release

# iOS release (requires Xcode + signing)
flutter build ios --release

# Web
flutter build web --release

# Lint / format
flutter analyze
dart format lib/

# Tests
flutter test
```

## Pre-release checklist (currently incomplete)

- [ ] Centralize API base URL into `ApiConfig` (technical-debt #1)
- [ ] Add Android signing config (`key.properties`, `build.gradle` signingConfigs)
- [ ] Add iOS provisioning profile + App Store Connect entry
- [ ] Verify Info.plist / AndroidManifest permissions match actual feature usage (both files currently dirty)
- [ ] Add crash reporting (Crashlytics / Sentry)
- [ ] Add at least smoke tests beyond `widget_test.dart`
- [ ] Set up CI (GitHub Actions: `flutter analyze` + `flutter test` on PR)
- [x] ~~Move `hand_landmarker.task` to `Assets/models/`~~ (done in `ef92bb5`)
- [x] ~~Add `build_output*.txt` to `.gitignore`~~ (done in working tree per `progress.md`, not yet committed)
- [ ] Push `ef92bb5` to `origin/main` — currently blocked by Windows Credential Manager caching wrong GitHub account (`Al-zahraaahmed` vs. `Al-zahraa-ahmed`); commit is only local
- [ ] Commit the 43-file audit-pass batch (auth hardening, Core utilities, OtpCubit consolidation, Profile fixes) — see [technical-debt.md](technical-debt.md) #9

## Health check

N/A — this is a mobile/desktop client app. The closest proxy is "app launches and reaches `LearingHome` / `Translationhome` / `Onboardingscreen` without crashing", which depends on:
1. `CacheHelper.init()` succeeds
2. `SignLanguageClassifier.instance.initialize()` does not block UI (it's `await`-less in `main()` by design)
3. `ProfileCubit().getMainData()` either succeeds or fails gracefully when offline / no token
